export let Swing: number[] = [];
export let Swang: number[] = [];

for (let i = 0; i <= 75; i++) {
    const num = i * 2;
    num <= 50 && Swang.push(num);
    Swing.push(num);
}

for (let i = 74; i >= 0; i--) {
    const num = i * 2;
    num <= 50 && Swang.push(num);
    Swing.push(num);
}
